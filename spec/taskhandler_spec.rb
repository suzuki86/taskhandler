require 'spec_helper'

describe Taskhandler do
  it 'has a version number' do
    expect(Taskhandler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
